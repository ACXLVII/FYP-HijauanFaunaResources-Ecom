# Deployment Commands

# 1. Check what changed
git status

# 2. Add all changes (code + models)
git add .

# 3. Commit with descriptive message
git commit -m "Add multi-placement AR feature + optimized artificial grass models (6MB each)"

# 4. Push to production
git push origin main

# 5. Wait for Vercel deployment (2-3 minutes)
# 6. Clear browser cache on your test devices
# 7. Test AR on both iOS and Android!








